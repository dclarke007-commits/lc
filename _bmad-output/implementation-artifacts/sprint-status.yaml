# generated: 2026-07-15
# last_updated: 2026-07-16
# project: LovesCleaning
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# Action Item Status:
#   - open: Committed during a retrospective, not yet addressed
#   - in-progress: Actively being worked on
#   - done: Completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)
# - Retrospective appends its action items to action_items; sprint-status surfaces open ones

generated: 2026-07-15
last_updated: 2026-07-19T03:50
project: LovesCleaning
# note: Epic 2 COMPLETE (dev + code-review, all 4 stories done) 2026-07-16 | 1-7 done (dev + code-review complete) 2026-07-16 — Epic 1 dev COMPLETE
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # --- Epic 1: Operator's Book — Foundation + Manual Scheduling ---
  epic-1: done
  1-1-authenticated-shell-operator-seed: done
  1-2-create-edit-client-records: done
  1-3-configure-availability-caps: done
  1-4-direct-booking-cap-enforcement-one-winner-concurrency: done
  1-5-mark-job-outcome-completed-no-show: done
  1-6-cancel-reschedule-capacity-release: done
  1-7-forecasting-lite-room-left-day-maxed-nearest-open: done
  epic-1-retrospective: done

  # --- Epic 2: Messaging Engine — Draft + Tap-to-Send ---
  epic-2: done
  2-1-operator-editable-message-templates: done
  2-2-compose-messagedraft-deep-link-delivery: done
  2-3-dispatch-logging-drafted-vs-dispatched-idempotent: done
  2-4-booking-confirmation-message: done
  epic-2-retrospective: done

  # --- Epic 3: Rebooking & Win-Back — Plug the Revenue Leak ---
  epic-3: done
  3-1-client-booking-surface-per-client-link: done
  3-2-known-client-direct-confirm-booking: done
  3-3-one-tap-rebooking-proposal-message: done
  3-4-post-job-nudge-rebooking-tracking: done
  3-5-lapse-detection-expected-next-date-gone-cold: done
  3-6-win-back-action: done
  epic-3-retrospective: done

  # --- Epic 4: Client Self-Booking — Plug the Inquiry Leak ---
  epic-4: done
  4-1-public-self-serve-link-qr: done
  4-2-new-client-public-booking-provisional-record-pending-request: done
  4-3-operator-approval-queue: done
  4-4-log-inquiry-with-source: done
  epic-4-retrospective: done

  # --- Epic 5: Cash Ledger — Plug the Payment Leak ---
  epic-5: done
  5-1-per-job-payment-status: done
  5-2-outstanding-aggregation-who-owes: done
  5-3-payment-reminder-draft: done
  5-4-mark-paid: done
  epic-5-retrospective: done

  # --- Epic 6: Leak-Detector Dashboard + Data Export ---
  epic-6: done
  6-1-single-screen-dashboard-core: done
  6-2-leak-indicators: done
  6-3-gone-cold-list-with-win-back-access: done
  6-4-csv-export: done
  epic-6-retrospective: done

action_items:
  - epic: 3
    action: "Introduce a named hasFutureBooking (booked-only) predicate for lapse, distinct from capacity.consumesSlot; document the intentional divergence at both sites"
    owner: "Developer"
    status: done
    resolution: "Done (chore/epic3-6-open-debt): extracted derive.hasFutureBooking(jobs, today) (booked-only) and routed goneCold through it; DIVERGENCE NOTE added at BOTH sites (the predicate's own doc + a note at capacity.consumesSlot explaining why lapse must NOT reuse the slot-consuming set — a today/future no-show is the lapse signal). Unit-tested in tests/derive.test.ts (narrower-than-consumesSlot cases)."
  - epic: 3
    action: "Add a nonce-keying convention to compose.ts docs: nonces key on the event instance (anchor/spell), never a bare entity id"
    owner: "Developer"
    status: done
    resolution: "Done (chore/epic3-6-open-debt): added a 'Dispatch-nonce keying convention' doc block above the nonce helpers in compose.ts — nonces key on the EVENT INSTANCE (anchor job id / cold-spell expectedNextDate), never a bare entity id, with the under-count failure mode that motivates it and a rule for new helpers."
  - epic: 3
    action: "Before Epic 6 dashboard (FR23) surfaces lapse at scale, address deferred perf: bounded/per-client listJobs projection, drop unused clientName join, de-dupe winback double-load"
    owner: "Developer"
    status: done
  - epic: 3
    action: "Establish a fail-pattern-uniformity check for RSC read surfaces (every read path fail-closed/open consistently, AD-8)"
    owner: "Developer"
    status: done
    resolution: "Done (chore/epic3-6-open-debt): audit confirmed all read surfaces ALREADY fail-closed (bare-value readers propagate/rethrow, ActionResult readers return fail('owner-unresolved') — none degrade to []/DEFAULT on auth failure). Locked it with tests/read-surface-fail-uniformity.test.ts (drives requireOwnerId to reject, asserts all 13 zero-arg bare readers reject + getPublicBookingLink returns a data-less failure) and codified the convention in lib/auth/requireOwnerId.ts."
  - epic: 3
    action: "Carry 3-1 token replay/revocation rigor + adversarial and security review into Epic 4 public self-serve (4-1/4-2)"
    owner: "Developer"
    status: done
  - epic: 4
    action: "Manual logInquiry (4.4) has no double-submit dedup — a double-tapped anonymous manual log inflates the inquiry→booking denominator. Accepted for single-operator v1; Epic 6 FR24 dashboard should dedupe manual double-logs (or add a per-render submit nonce + partial index if it matters)."
    owner: "Developer"
    status: done
    resolution: "Done (chore/epic3-6-open-debt): per-render submit nonce + partial unique index. inquiries/page.tsx embeds a fresh randomUUID() per render as a hidden submitNonce; logInquiry passes it to insertInquiry, which ON CONFLICT DO NOTHING dedups on the new partial index inquiry_owner_submit_manual_uq (owner, session_nonce WHERE source<>'link') — reusing the existing session_nonce column, disjoint from the link index. A double-tap collapses to one row (returns the existing); a nonce-less log keeps legacy insert-always. Migration 0014 (single CREATE UNIQUE INDEX). Tests in tests/inquiry-log.test.ts."
  - epic: 4
    action: "Public write surfaces (4.2 submit, 4.4 log) have no rate limiting — accepted gap (LOCKED). Add per-IP throttle + provisional-row cap before/when the app becomes multi-tenant or lower-trust."
    owner: "Developer"
    status: done
    resolution: "Done (chore/security-defense-in-depth): per-IP sliding-window throttle (lib/security/rateLimit + requestIp) at the book action boundary (confirm + submit) + DB-backed provisional-row cap (queries.countRecentPendingRequests, 30/10min per owner) in submitPublicRequestResult. In-memory throttle documented as best-effort/per-instance; DB cap is the durable, instance-spanning half. tests/rate-limit.test.ts."
  - epic: 6
    action: "Owner-isolation DB-integration tests for dashboard/export data paths (listJobsForMetrics, listJobsForExport, listInquiries). Bogus owner_id returns none; seeded owner sees its rows. (operator_singleton blocks a 2nd live tenant, so filter-value coverage is the achievable proof.) tests/dashboard-owner-scope.test.ts."
    owner: "Developer"
    status: done
  - epic: 6
    action: "Add explicit caughtCold/lapse boundary tests to tests/derive.test.ts: today-8 exclusion + a year rollover case (adversarial testing gap)."
    owner: "Developer"
    status: done
  - epic: 6
    action: "Cross-action today consistency test: assert lapsed count, caught-cold count, and gone-cold list agree within one render (the renderNow fix is untested). Deferred — needs React request-context simulation to exercise cache() dedup; fragile as a direct unit test."
    owner: "Developer"
    status: done
    resolution: "Done (chore/security-defense-in-depth): pinned the invariant at the pure derive layer instead of simulating React cache() — for one `today`, repeatVsLapsedCounts.lapsed === goneColdList length and caughtColdThisWeek ⊆ that set; plus a midnight-straddle case proving two different `today` values desync (why the shared renderNow instant is load-bearing). tests/dashboard-lapse-consistency.test.ts."
  - epic: 6
    action: "Repeat/Lapsed label nuance — clarified as INDEPENDENT counts (not a partition): '·' separator, '(independent)' note, tile relabelled 'Repeat & lapsed'. app/(operator)/page.tsx."
    owner: "Developer"
    status: done
  - epic: 6
    action: "Defense-in-depth auth decision (app-wide): whether operator Server Actions should verify session in-action (shared requireOperatorSession) vs rely solely on proxy.ts middleware. Pre-existing AD-6 pattern, not an Epic-6 regression."
    owner: "Project Lead + Developer"
    status: done
    resolution: "Decided IN-ACTION verification + implemented (chore/security-defense-in-depth): lib/auth/requireOwnerId verifies the HMAC session cookie (signature + expiry) in-action and returns session.sub as the owner_id; all 12 operator action files swapped getOwnerId()→requireOwnerId() so a matcher gap / direct action invocation can never run owner-scoped work without a verified session. proxy.ts remains the navigation gate. getOwnerId() retained as the DB primitive (seed/tests). Real impl unit-tested in tests/require-owner-id.test.ts; integration tests delegate via a setup.ts mock."
  - epic: 1
    action: "Unify capacity.ts private week-arithmetic onto clock.ts (single-source the Mon-Sun week helpers; capacity keeps private copies pre-existing)."
    owner: "Developer"
    status: done
  - epic: 1
    action: "Drop the redundant non-unique owner_id index fully covered by the unique index (new migration; micro-cleanup)."
    owner: "Developer"
    status: done
  - epic: 1
    action: "zonedWallToUtc drops milliseconds when measuring the offset — latent, no current ms caller; add guard or test before one appears."
    owner: "Developer"
    status: done
  - epic: 1
    action: "Timezone is free-text on a phone-first surface; replace with a zero-JS <select> of common zones (NFR1 fit)."
    owner: "Developer"
    status: done
  - epic: 2
    action: "Introduce a shared safeErrorMessage(map, key) (Object.hasOwn-guarded) and route all error-message maps through it — kills the prototype-key crash class app-wide; capacityErrors.ts still latent."
    owner: "Developer"
    status: done
  - epic: 2
    action: "Add a nonce-keying convention to compose.ts docs: nonces key on the event instance (anchor/spell), never a bare entity id."
    owner: "Developer"
    status: done
    resolution: "Done (chore/epic3-6-open-debt): same fix as the epic-3 duplicate of this item — 'Dispatch-nonce keying convention' doc block added above the nonce helpers in compose.ts."
